import { deviation, mean } from 'd3';
import sp500 from '../data/parsed_SP500';

const generateMarketData = (firstYear, lastYear) => {

};

const getDataGeneratorInputVariables = (firstYear, lastYear) => {

};

export default generateMarketData;